*.log

# Python
__pycache__/
*.pyc
# Never commit tokens (they're stored in ~/.config/live-claude-ui by default,
# but ignore any local copies just in case)
tokens.json
.env

# Firmware secrets (never commit real creds/token) + build artifacts
firmware/src/secrets.h
firmware/build/
firmware/build-sb/
build/
build-sb/
